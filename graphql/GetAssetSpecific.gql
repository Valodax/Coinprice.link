query GetAssetSpecific($asset: String!, $timeFilter: BigInt!) {
  dataFeeds(where: { asset: $asset, denomination: "USD" }) {
    id
    phaseId
    live
    decimals
    prices(orderBy: roundId, orderDirection: asc, where: { blockTimestamp_gte: $timeFilter }) {
      id
      price
      roundId
      blockTimestamp
      blockNumber
    }
  }
}
